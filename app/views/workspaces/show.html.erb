<h1><%= @workspace.name %></h1>


<h3><%= @workspace.neighborhood %>
<%= @workspace.price %>€</h3>

<p><%= @workspace.address %></p>

<p>Hosted by <%= @workspace.user.username %></p>

<%= link_to "Edit coworking space", edit_workspace_path(@workspace)%>
<%= link_to "Delete coworking space", workspace_path(@workspace), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
